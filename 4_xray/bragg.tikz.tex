
\begin{tikzpicture}

%\useasboundingbox (-1.3,-1.2) rectangle (11.2,4.7);

\clip (-2.75,-0.5) rectangle ++ (5.5,3.2);

\tikzmath{\atom=0.1; \d=1.5; \wi=25;}

\foreach \v in {-2,-1,...,1}
{  
 \draw[fill=black]  ({\v * \d + \d/2},0) circle (\atom);
 \draw[fill=black]  ({\v * \d + \d/2},\d) circle (\atom);
 }
 
 \draw[dashed] (-3,0) -- ++ (6,0);
 \draw[dashed] (-3,\d) -- ++ (6,0);
 
\coordinate (O) at (0,0);
\coordinate (zwei) at (0,\d);
\coordinate (zweilinks) at (-3,\d);
\coordinate (zweilinksende) at ($ (zwei) + (180-\wi:3) $);

\coordinate (linksende) at ($ (O) + (180-\wi:2) $);
\coordinate (rechtsende) at ($ (O) + (\wi:3) $);


 \draw[thick] (O) -- (linksende) ; 
 \draw[thick] (O) -- (rechtsende) ; 
 \draw[thick]  (zwei) -- ++ (\wi:3); 
 \draw[thick]  (zwei) -- (zweilinksende); 
 
 \draw[dashed] (O) -- (zwei);
 
  \draw  (zwei) -- ($(O)!(zwei)!(linksende)$);
 \draw  (zwei) -- ($(O)!(zwei)!(rechtsende)$);
 \draw[line width=2pt, blue]   ($(O)!(zwei)!(linksende)$) -- (O)  -- node(dp) {} ($(O)!(zwei)!(rechtsende)$);
 
 \node[below ] at (O) {$2d \sin \Theta$};
 
 \draw [<->] (-2.5,0) --  node[anchor=west] {$d$} ++ (0,\d);
 
%\draw pic["$\Theta$",draw=black,<->,angle eccentricity=1.2,angle radius=1cm]
\pic [draw=black,<->,angle eccentricity=1.2,angle radius=1cm]
           {angle=zweilinksende--zwei--zweilinks} ;

\node[xshift=-12mm, yshift=2.5mm] at (zwei) {$\Theta$};

  \draw [<-] (-0.5, \d + 0.5) -- node[above] {$\mathbf{k}_0$} ++ (180-\wi:0.7);
  \draw [->] (0.5, \d + 0.5) -- node[above] {$\mathbf{k}$} ++ (\wi:0.7);


%\coordinate (links) at (intersection of (O)--(linksende)  and (zwei)--(linkshelp) );

  \end{tikzpicture}
