% \documentclass[margin=0mm]{standalone}
% \usepackage{tikz}
% \usepackage{pgfplots}
% \pgfplotsset{compat=newest}
% \usepackage{braket}

% \usepackage{currfile,hyperxmp}
% \usetikzlibrary{math,calc,matrix,fit,positioning,intersections}
% \usetikzlibrary{patterns,decorations.pathmorphing,decorations.markings}


% \usepackage{xfp}



% \begin{document}
  
\begin{tikzpicture}
%\useasboundingbox (-1.3,-1.2) rectangle (10.2,4.7);
%\draw (-1.3,-1.2) rectangle (11.2,4.7);

    \begin{axis}[no markers,  xlabel={pulse area $\theta$ ($\pi$)}, ylabel={emission (arb.u.)}, 
         width=112mm, height=50mm, 
         xmin = 0, xmax = 4, ymin=0,
    ]
    \addplot[mark=+, only marks, blue, mark size=1pt] table [x index=0, y index=1, col sep=comma] {\currfiledir data/Gibbs_1976_rabi_figure_data.csv};
    % digitized model of Gibbs
    %\addplot[no marks, line width=1pt, smooth] table [x index=0, y index=1, col sep=comma] {\currfiledir data/Gibbs_1976_rabi_figure_model.csv};

    % hand-made model by ML
    \addplot[domain=0:4, samples=400, line width=1pt] {(90 -x^2/2 ) * sin(deg(x  * pi / 1.95))^2 + x * 13 };
	

    \end{axis}
\end{tikzpicture}

%\end{document}